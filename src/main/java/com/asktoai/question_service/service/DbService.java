package com.asktoai.question_service.service;
/*
import com.asktoai.question_service.repository.ChatEntity;
import com.asktoai.question_service.repository.ChatRepository;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;


@Slf4j
@Service
public class DbService {
    private final ChatRepository chatRepository;

    public DbService(ChatRepository chatRepository) {
        this.chatRepository = chatRepository;
    }

    public void saveChat(String question) {
        log.info("Saving chat to db");
        ChatEntity chatEntity = new ChatEntity();
        //chatEntity.setId(1);
        chatEntity.setQuestion(question);
        chatRepository.save(chatEntity);
    }
}
*/
